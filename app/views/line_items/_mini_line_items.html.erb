<tr>
	<% if line_item == @current_item %>
	<td id="current-item">
	<% else %>
	<td>
	<% end %>
		<div class="quantity">
			<%= line_item.quantity %>
		</div>
		<%= line_item.product.name if line_item.product %>
		<%= line_item.malone_tune.name if line_item.malone_tune %>
	</td>
</tr>