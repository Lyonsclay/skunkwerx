<h2 id="title">Your Shopping Cart</h2>
<div class="buttons">
	<%= link_to 'Empty Cart', "/carts/#{cart.id}", method: :delete, data: { confirm: 'Are you sure?'} %>
	<%= link_to 'Continue Shopping', shopping %>
	<%= link_to 'Checkout', edit_cart_path(cart) %>
</div>
<%= form_tag remove_multiple_line_items_path, method: :post do %>
	<table class="line-items">
		<thead>
			<tr class="cart-heading">
				<td>Remove</td>
				<td id="products">Product(s)</td>
				<td>Qty.</td>
				<td>Total</td>
			</tr>
		</thead>
		<tbody>
		<%= render(cart.line_items) %>
		<tbody>
		<tfoot class="sub-total">
			<td><%= submit_tag "Update", class: 'buttons' %></td>
			<td style="text-align:right">Sub-total:</td>
			<td></td>
			<td><%= number_to_currency cart.total_price %></td>
		</tfoot>
	</table>
<% end %>
